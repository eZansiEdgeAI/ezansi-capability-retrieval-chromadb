version: '3.8'

# Raspberry Pi 4 overrides for the chromadb-retrieval capability stack.
# Usage:
#   podman-compose -f podman-compose.yml -f podman-compose.pi4.yml up -d --build

services:
  chroma:
    mem_limit: 1500m
    cpus: 2.0

  chromadb-retrieval:
    mem_limit: 1500m
    cpus: 2.0
