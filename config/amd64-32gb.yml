# AMD64 (32GB) suggested runtime profile.
# Applied via: podman-compose -f podman-compose.yml -f podman-compose.amd64.yml up -d --build
profile: amd64-32gb
resources:
  cpu_cores: 4
  ram_mb: 4000
  storage_mb: 4000
embedding:
  model: BAAI/bge-small-en-v1.5
