# Raspberry Pi 5 (16GB) suggested runtime profile.
# Applied via: podman-compose -f ./podman-compose.yml -f ./compose/pi5.yml up -d --build
profile: pi5-16gb
resources:
  cpu_cores: 3
  ram_mb: 2000
  storage_mb: 2000
embedding:
  model: BAAI/bge-small-en-v1.5
